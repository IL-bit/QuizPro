import { createAction } from "@reduxjs/toolkit";

export const togglePopup = createAction('TOGGLEPOPUP');
export const closePopup = createAction('CLOSEPOPUP');
export const toggleButton = createAction('TOGGLEBUTTON');
export const log_in = createAction('LOG_IN');
export const islog = createAction('ISLOG');